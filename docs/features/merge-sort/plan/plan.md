# Merge Sort Feature Plan

## Goals
- Add merge sort visualization with reusable controls.

## Implementation
- Build pure merge-sort step generation in `src/algorithms/sortingSteps.ts`.
- Add `src/algorithm/MergeSort.tsx` wrapper for shared sorting visualizer.

## Acceptance Criteria
- Merge Sort selection renders and sorts arrays in both orders.
